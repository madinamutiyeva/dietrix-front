# Absolute backend URL. Leave empty in development to use the Vite proxy
# (in that case the app calls /api/* and vite.config.ts forwards to localhost:8080).
#
# In production set this to the public origin of your backend, e.g.
#   VITE_API_URL=https://api.dietrix.app
VITE_API_URL=

